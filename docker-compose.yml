version: '3'
services:
  web:
    build: .
    image: ${DOCKER_USER_ID}/flask
  production:
    image: ${DOCKER_USER_ID}/flask:${BUILD_NUMBER}
    command: python app.py
    volumes:
      - .:/app
    ports:
      - "80:80"